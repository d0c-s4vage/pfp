BigEndian();
 
typedef struct {
    uint32 length;
    char type[4];
    byte data[length];
    uint32 crc <watch=type;data, update=WatchCrc32>;
} PngChunk;
 
struct {
    uchar signature[8];
 
    while (!FEof()) {
        PngChunk chunks;
    }
} PNG;
